[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:${PORT:-8080} --workers 1 --timeout 120 --log-level debug --capture-output --enable-stdio-inheritance webhook:app"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
